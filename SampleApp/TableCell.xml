<TableCell
    detailTextLabel.highlightedTextColor="white"
    detailTextLabel.text="This cell is defined in TableCell.xml"
    detailTextLabel.textColor="#aaa"
    height="auto + 20"
    reuseIdentifier="standaloneCell"
    selectedBackgroundView.backgroundColor="colors.red"
    style="subtitle"
    textLabel.highlightedTextColor="white"
    textLabel.text="Title #{row}">

    <UIImageView
        highlightedImage="{whiteImage}"
        image="{image}"
        right="100% - 20"
        tintColor="colors.red"
        top="20"
    />
    <UIButton
            touchUpInside="buttonClicked"
            backgroundColor="red"
            title="{buttonText}"
            outlet="button"
            top="previous.bottom + 52"
            height="auto"
            width="auto"
            left="18"
        />
</TableCell>
